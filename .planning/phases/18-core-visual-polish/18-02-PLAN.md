---
phase: 18-core-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ['18-01']
files_modified:
  - src/routes/index.tsx
  - src/routes/matches/index.tsx
  - src/routes/opportunities/index.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - 'Home page has warm gradient background, not flat gray'
    - 'Matches list page uses warm background with proper typography'
    - 'Opportunities list page uses warm background'
    - 'All headings on these pages use Lora display font'
  artifacts:
    - path: 'src/routes/index.tsx'
      provides: 'Home page with warm visual treatment'
      contains: 'GradientBg'
    - path: 'src/routes/matches/index.tsx'
      provides: 'Matches list with warm treatment'
      contains: 'GradientBg'
    - path: 'src/routes/opportunities/index.tsx'
      provides: 'Opportunities list with warm treatment'
      contains: 'GradientBg'
  key_links:
    - from: 'src/routes/index.tsx'
      to: 'src/components/layout/GradientBg.tsx'
      via: 'import and usage'
      pattern: 'import.*GradientBg'
    - from: 'src/routes/matches/index.tsx'
      to: 'src/components/layout/GradientBg.tsx'
      via: 'import and usage'
      pattern: 'import.*GradientBg'
---

<objective>
Apply warm visual treatment to the three highest-traffic list pages: Home, Matches, and Opportunities.

Purpose: Transform these core pages from flat gray backgrounds to warm atmospheric backgrounds with proper typography hierarchy.

Output: Three updated route files with GradientBg backgrounds and font-display/font-body typography.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-core-visual-polish/18-RESEARCH.md
@.planning/phases/18-core-visual-polish/18-CONTEXT.md
@.planning/phases/18-core-visual-polish/18-01-SUMMARY.md

# Files to update

@src/routes/index.tsx
@src/routes/matches/index.tsx
@src/routes/opportunities/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Home page with warm treatment</name>
  <files>src/routes/index.tsx</files>
  <action>
Update the Home page (index.tsx) to use warm visual treatment:

1. Import GradientBg:

   ```tsx
   import { GradientBg } from '~/components/layout/GradientBg'
   ```

2. Replace the outer div in Home():

   ```tsx
   // FROM:
   <div className="min-h-screen bg-background">

   // TO:
   <GradientBg variant="subtle">
   ```

   Close tag: `</GradientBg>` instead of `</div>`

3. Update typography in LandingPage():
   - h1: Change `font-mono` to `font-display`
   - Keep text sizes, just swap font family

4. Update typography in Dashboard():
   - h2 section titles: Add `font-display` class
   - Keep `font-semibold`, add `font-display`

5. Update EmptyStatePrompt and EventsEmptyState components:
   - h3 titles: Add `font-display` class
   - Change `bg-slate-100` to `bg-cream-100` for icon backgrounds
   - Change `text-slate-400` icons to `text-coral-400` for warmth

Per CONTEXT.md: "Compact & dense" - keep existing spacing, don't add padding.
</action>
<verify>Run `bun run lint`. Verify GradientBg import exists. Check that font-mono is removed: `grep "font-mono" src/routes/index.tsx` should return nothing.</verify>
<done>Home page uses GradientBg variant="subtle", headings use font-display, empty states have warm cream/coral accents.</done>
</task>

<task type="auto">
  <name>Task 2: Update Matches list page with warm treatment</name>
  <files>src/routes/matches/index.tsx</files>
  <action>
Update the Matches list page to use warm visual treatment:

1. Import GradientBg:

   ```tsx
   import { GradientBg } from '~/components/layout/GradientBg'
   ```

2. Replace outer div in MatchesPage():

   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```

   Close tag: `</GradientBg>` instead of `</div>`

3. Update typography throughout:
   - All `font-bold` on h1/h2 headings -> `font-display font-semibold`
   - `text-slate-900` stays (good contrast on warm background)
   - `text-slate-500` body text -> `text-muted-foreground` (semantic)

4. Update "Create Your Profile First" card:
   - h1: Add `font-display`
   - Icon background: `bg-primary/10` is fine (already coral-based)

5. Update "Finding Your Matches" card:
   - h1: Add `font-display`

6. Update "No matches yet" empty state:
   - h2: Add `font-display`
   - Icon background: Change `bg-slate-100` to `bg-cream-100`

7. Update main matches header:
   - h1 "Your Matches": Add `font-display`
     </action>
     <verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/matches/index.tsx` returns nothing. `grep "GradientBg" src/routes/matches/index.tsx` returns import and usage.</verify>
     <done>Matches list page uses GradientBg, all headings use font-display, slate backgrounds replaced with cream/semantic tokens.</done>
     </task>

<task type="auto">
  <name>Task 3: Update Opportunities list page with warm treatment</name>
  <files>src/routes/opportunities/index.tsx</files>
  <action>
Update the Opportunities list page to use warm visual treatment:

1. Import GradientBg:

   ```tsx
   import { GradientBg } from '~/components/layout/GradientBg'
   ```

2. Replace outer div in OpportunitiesPage():

   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```

   Close tag: `</GradientBg>` instead of `</div>`

3. Update typography:
   - h1 "Opportunities": Change `font-bold` to `font-display font-semibold`
   - Keep `text-slate-900` for good contrast
   - `text-slate-600` count text -> `text-muted-foreground`

This page is simpler - mainly background change and heading typography.
</action>
<verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/opportunities/index.tsx` returns nothing. `grep "font-display" src/routes/opportunities/index.tsx` shows usage on heading.</verify>
<done>Opportunities list page uses GradientBg variant="subtle", heading uses font-display, body text uses semantic color tokens.</done>
</task>

</tasks>

<verification>
- [ ] `grep -l "GradientBg" src/routes/index.tsx src/routes/matches/index.tsx src/routes/opportunities/index.tsx` shows all 3 files
- [ ] `grep "bg-slate-50" src/routes/index.tsx src/routes/matches/index.tsx src/routes/opportunities/index.tsx` returns nothing
- [ ] `grep "font-display" src/routes/index.tsx` shows usage on headings
- [ ] `bun run lint` passes
- [ ] `bun run build` succeeds
</verification>

<success_criteria>

- Home, Matches list, and Opportunities list pages all use GradientBg component
- No `bg-slate-50` remains in these files
- Headings use `font-display` for Lora display font
- Body text uses semantic color tokens (muted-foreground)
- All lint and build checks pass
  </success_criteria>

<output>
After completion, create `.planning/phases/18-core-visual-polish/18-02-SUMMARY.md`
</output>
