---
phase: 18-core-visual-polish
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/routes/matches/$id.tsx
  - src/routes/opportunities/$id.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Match detail page has warm background instead of gray"
    - "Opportunity detail page has warm background instead of gray"
    - "Both detail pages use Lora font for headings"
    - "Typography hierarchy is consistent with list pages"
  artifacts:
    - path: "src/routes/matches/$id.tsx"
      provides: "Match detail with warm treatment"
      contains: "GradientBg"
    - path: "src/routes/opportunities/$id.tsx"
      provides: "Opportunity detail with warm treatment"
      contains: "GradientBg"
  key_links:
    - from: "src/routes/matches/$id.tsx"
      to: "src/components/layout/GradientBg.tsx"
      via: "import and usage"
      pattern: "import.*GradientBg"
    - from: "src/routes/opportunities/$id.tsx"
      to: "src/components/layout/GradientBg.tsx"
      via: "import and usage"
      pattern: "import.*GradientBg"
---

<objective>
Apply warm visual treatment to Match detail and Opportunity detail pages.

Purpose: Ensure detail pages have consistent visual treatment with their parent list pages, creating seamless navigation experience.

Output: Two updated detail route files with GradientBg backgrounds and proper typography.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-core-visual-polish/18-RESEARCH.md
@.planning/phases/18-core-visual-polish/18-CONTEXT.md
@.planning/phases/18-core-visual-polish/18-01-SUMMARY.md

# Files to update
@src/routes/matches/$id.tsx
@src/routes/opportunities/$id.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Match detail page with warm treatment</name>
  <files>src/routes/matches/$id.tsx</files>
  <action>
Update the Match detail page to use warm visual treatment:

1. Import GradientBg:
   ```tsx
   import { GradientBg } from "~/components/layout/GradientBg";
   ```

2. Find the main container div (likely `<div className="min-h-screen bg-slate-50">`) and replace:
   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```
   Ensure closing tag matches.

3. Update typography throughout the component:
   - Main heading (opportunity title): Add `font-display` class
   - Section headings (h2/h3): Add `font-display` class
   - Change `font-bold` to `font-semibold` on headings (Lora looks better at semibold)
   - Keep `text-slate-900` for main text (good contrast)
   - Change `text-slate-500/600` to `text-muted-foreground` where appropriate

4. Update any empty state or loading cards:
   - Icon backgrounds: `bg-slate-100` -> `bg-cream-100`
   - Icon colors: Consider `text-coral-400` for warmth

Per CONTEXT.md: Claude's discretion for detail page header prominence - keep it clean, don't over-embellish.
  </action>
  <verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/matches/\\$id.tsx` returns nothing (note: escape $ in shell). `grep "GradientBg" src/routes/matches/\\$id.tsx` shows import.</verify>
  <done>Match detail page uses GradientBg, headings use font-display, warm color tokens applied.</done>
</task>

<task type="auto">
  <name>Task 2: Update Opportunity detail page with warm treatment</name>
  <files>src/routes/opportunities/$id.tsx</files>
  <action>
Update the Opportunity detail page to use warm visual treatment:

1. Import GradientBg:
   ```tsx
   import { GradientBg } from "~/components/layout/GradientBg";
   ```

2. Find the main container div and replace:
   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```

3. Update typography:
   - Page/opportunity title: Add `font-display` class
   - Remove any `font-mono` usage (research noted this page has font-mono)
   - Section headings: Add `font-display`
   - Change `font-bold` to `font-semibold` on headings

4. Update color tokens:
   - `text-slate-*` for body text -> `text-muted-foreground` or keep slate-600/700 for readability
   - Any `bg-slate-100` backgrounds -> `bg-cream-100`

5. Cards within the page:
   - They will automatically have warm shadow from Plan 01's Card update
   - No additional changes needed for card shadows
  </action>
  <verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/opportunities/\\$id.tsx` returns nothing. `grep "font-mono" src/routes/opportunities/\\$id.tsx` returns nothing.</verify>
  <done>Opportunity detail page uses GradientBg, font-mono removed, headings use font-display, typography consistent with rest of app.</done>
</task>

</tasks>

<verification>
- [ ] `grep -l "GradientBg" src/routes/matches/\\$id.tsx src/routes/opportunities/\\$id.tsx` shows both files
- [ ] `grep "bg-slate-50" src/routes/matches/\\$id.tsx src/routes/opportunities/\\$id.tsx` returns nothing
- [ ] `grep "font-mono" src/routes/opportunities/\\$id.tsx` returns nothing
- [ ] `bun run lint` passes
- [ ] `bun run build` succeeds
</verification>

<success_criteria>
- Match detail and Opportunity detail pages use GradientBg component
- No `bg-slate-50` remains in these files
- No `font-mono` in opportunity detail (was noted in research)
- Headings use `font-display` for Lora display font
- All lint and build checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-core-visual-polish/18-03-SUMMARY.md`
</output>
