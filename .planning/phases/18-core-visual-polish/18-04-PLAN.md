---
phase: 18-core-visual-polish
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/routes/profile/index.tsx
  - src/routes/profile/edit.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Profile view page has warm background instead of gray"
    - "Profile edit page has warm background instead of gray"
    - "Both profile pages use Lora font for headings"
    - "Profile section cards have coral-tinted shadows"
  artifacts:
    - path: "src/routes/profile/index.tsx"
      provides: "Profile view with warm treatment"
      contains: "GradientBg"
    - path: "src/routes/profile/edit.tsx"
      provides: "Profile edit with warm treatment"
      contains: "GradientBg"
  key_links:
    - from: "src/routes/profile/index.tsx"
      to: "src/components/layout/GradientBg.tsx"
      via: "import and usage"
      pattern: "import.*GradientBg"
    - from: "src/routes/profile/edit.tsx"
      to: "src/components/layout/GradientBg.tsx"
      via: "import and usage"
      pattern: "import.*GradientBg"
---

<objective>
Apply warm visual treatment to Profile view and Profile edit pages.

Purpose: Transform profile management pages from flat gray to warm atmospheric backgrounds, maintaining visual consistency across the entire app.

Output: Two updated profile route files with GradientBg backgrounds and proper typography.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-core-visual-polish/18-RESEARCH.md
@.planning/phases/18-core-visual-polish/18-CONTEXT.md
@.planning/phases/18-core-visual-polish/18-01-SUMMARY.md

# Files to update
@src/routes/profile/index.tsx
@src/routes/profile/edit.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Profile view page with warm treatment</name>
  <files>src/routes/profile/index.tsx</files>
  <action>
Update the Profile view page to use warm visual treatment:

1. Import GradientBg:
   ```tsx
   import { GradientBg } from "~/components/layout/GradientBg";
   ```

2. Replace outer div in ProfilePage():
   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```
   Ensure closing tag matches.

3. Update typography:
   - Main heading (profile name): Add `font-display` class, keep `font-bold` or change to `font-semibold`
   - Section card headings (h2 "Basic Information", "Education", etc.): Add `font-display` to the h2 elements
   - "Create Your Profile" card h1: Add `font-display`

4. Update empty state styling:
   - Icon backgrounds in "Create Your Profile" card: `bg-primary/10` is already coral-based, keep it
   - Empty state text: Keep `text-slate-400 italic` or change to `text-muted-foreground italic`

5. Update completeness banner:
   - Keep `bg-amber-50 border-amber-200` - this is semantic (warning/attention)
   - Text colors can stay amber-based

6. Update section icons:
   - Icon colors are `text-slate-400` - consider changing to `text-coral-400` for subtle warmth
   - This is optional - slate-400 works fine on warm background

Per CONTEXT.md: "Tight grouping" - keep existing spacing compact.
  </action>
  <verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/profile/index.tsx` returns nothing. `grep "font-display" src/routes/profile/index.tsx` shows multiple uses.</verify>
  <done>Profile view page uses GradientBg, section headings use font-display, icon colors optionally warmed.</done>
</task>

<task type="auto">
  <name>Task 2: Update Profile edit page with warm treatment</name>
  <files>src/routes/profile/edit.tsx</files>
  <action>
Update the Profile edit page to use warm visual treatment:

1. Import GradientBg:
   ```tsx
   import { GradientBg } from "~/components/layout/GradientBg";
   ```

2. Find the main container and replace:
   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```

3. Update typography:
   - Page heading "Edit Profile" or similar: Add `font-display`
   - Section headings within the form: Add `font-display`
   - Keep form labels as-is (Plus Jakarta Sans body font is appropriate for form labels)

4. Form styling:
   - Form inputs and selects will inherit from shadcn defaults which are already neutral
   - Cards containing form sections will automatically get warm shadows from Plan 01
   - Keep form spacing compact per CONTEXT.md

5. Update any slate backgrounds:
   - `bg-slate-100` helper text backgrounds -> `bg-cream-100`
   - `bg-slate-50` section backgrounds -> remove or use `bg-cream-50`

6. Buttons and actions:
   - Keep primary button coral colors (already warm)
   - Outline/secondary buttons are neutral, which is fine
  </action>
  <verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/profile/edit.tsx` returns nothing (as page background). `grep "GradientBg" src/routes/profile/edit.tsx` shows import and usage.</verify>
  <done>Profile edit page uses GradientBg, form section headings use font-display, slate backgrounds replaced with cream variants where appropriate.</done>
</task>

</tasks>

<verification>
- [ ] `grep -l "GradientBg" src/routes/profile/index.tsx src/routes/profile/edit.tsx` shows both files
- [ ] `grep "bg-slate-50" src/routes/profile/index.tsx src/routes/profile/edit.tsx` returns nothing (for page backgrounds)
- [ ] `grep "font-display" src/routes/profile/index.tsx` shows usage on headings
- [ ] `bun run lint` passes
- [ ] `bun run build` succeeds
</verification>

<success_criteria>
- Profile view and Profile edit pages use GradientBg component
- Page background `bg-slate-50` removed from both files
- Headings use `font-display` for Lora display font
- Form sections maintain compact spacing
- All lint and build checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-core-visual-polish/18-04-SUMMARY.md`
</output>
