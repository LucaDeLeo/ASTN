---
phase: 18-core-visual-polish
plan: 05
type: execute
wave: 2
depends_on: ['18-01']
files_modified:
  - src/routes/admin/route.tsx
  - src/routes/admin/index.tsx
  - src/routes/orgs/index.tsx
autonomous: false

must_haves:
  truths:
    - 'Admin pages have dot grid pattern background differentiating them from user pages'
    - 'Organizations page has warm background'
    - 'Login page warmth is preserved (not regressed)'
    - 'All pages feel visually consistent with warm treatment'
  artifacts:
    - path: 'src/routes/admin/route.tsx'
      provides: 'Admin layout with dot grid pattern'
      contains: 'backgroundImage'
    - path: 'src/routes/orgs/index.tsx'
      provides: 'Organizations page with warm treatment'
      contains: 'GradientBg'
  key_links:
    - from: 'src/routes/admin/route.tsx'
      to: 'src/styles/app.css'
      via: 'cream color token usage'
      pattern: 'cream'
    - from: 'src/routes/orgs/index.tsx'
      to: 'src/components/layout/GradientBg.tsx'
      via: 'import and usage'
      pattern: 'import.*GradientBg'
---

<objective>
Apply warm visual treatment to Admin layout/dashboard and Organizations page, with a visual verification checkpoint.

Purpose: Complete the visual polish across all remaining pages, including the distinct admin dot grid pattern per CONTEXT.md. Verify the entire visual system works cohesively.

Output: Updated admin and orgs routes with appropriate warm treatment, visual verification of all pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-core-visual-polish/18-RESEARCH.md
@.planning/phases/18-core-visual-polish/18-CONTEXT.md
@.planning/phases/18-core-visual-polish/18-01-SUMMARY.md

# Files to update

@src/routes/admin/route.tsx
@src/routes/admin/index.tsx
@src/routes/orgs/index.tsx

# Reference for login (verify preserved)

@src/routes/login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Admin layout with dot grid pattern</name>
  <files>src/routes/admin/route.tsx</files>
  <action>
Update the Admin layout to use a dot grid pattern background per CONTEXT.md decision: "Admin pages get a dot grid pattern - visual differentiation that signals different part of the app."

1. Find the main container in the Authenticated section:

   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <div
     className="min-h-screen"
     style={{
       backgroundImage: `radial-gradient(circle, oklch(0.7 0.05 30 / 0.12) 1px, transparent 1px)`,
       backgroundSize: '24px 24px',
       backgroundColor: 'oklch(0.99 0.01 90)' // cream-50 equivalent
     }}
   >
   ```

2. Update typography:
   - "ASTN Admin" link: Add `font-display` class
   - Navigation links can stay as Plus Jakarta Sans (body font for nav is fine)

3. Header styling:
   - Keep `bg-white` for header - creates visual anchor against dotted background
   - Header bottom border stays for separation

Note: We're NOT using GradientBg here - admin gets inline styles for the dot grid pattern. This is intentional differentiation.
</action>
<verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/admin/route.tsx` returns nothing. `grep "backgroundImage" src/routes/admin/route.tsx` shows dot grid pattern.</verify>
<done>Admin layout uses dot grid pattern with cream-tinted background, creating visual distinction from user-facing pages.</done>
</task>

<task type="auto">
  <name>Task 2: Update Organizations page with warm treatment</name>
  <files>src/routes/orgs/index.tsx</files>
  <action>
Update the Organizations page to use warm visual treatment:

1. Import GradientBg:

   ```tsx
   import { GradientBg } from '~/components/layout/GradientBg'
   ```

2. Find the main container and replace:

   ```tsx
   // FROM:
   <div className="min-h-screen bg-slate-50">

   // TO:
   <GradientBg variant="subtle">
   ```

3. Update typography:
   - Page heading: Add `font-display` class
   - Section headings if any: Add `font-display`
   - Keep organization card content as body font

4. Update any slate-colored elements:
   - `text-slate-*` for muted text -> `text-muted-foreground`
   - `bg-slate-100` backgrounds -> `bg-cream-100`

5. Map integration (if visible on this page):
   - Map container styling can stay as-is
   - Surrounding elements should use warm tokens

The page may have OrgCard components - those will automatically get warm shadows from the Card update in Plan 01.
</action>
<verify>Run `bun run lint`. Verify: `grep "bg-slate-50" src/routes/orgs/index.tsx` returns nothing. `grep "GradientBg" src/routes/orgs/index.tsx` shows import and usage.</verify>
<done>Organizations page uses GradientBg, typography updated, org cards have warm shadows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete warm visual treatment across all 9 main pages:
- Foundation: GradientBg component + warm shadow tokens
- List pages: Home, Matches list, Opportunities list
- Detail pages: Match detail, Opportunity detail
- Profile pages: Profile view, Profile edit
- Admin: Dot grid pattern layout
- Organizations: Warm gradient background
- Login: Preserved existing warm treatment</what-built>
  <how-to-verify>
1. Start dev server: `bun run dev`
2. Visit each page and verify warm visual treatment:
   - http://localhost:5173/ - Home: Subtle warm gradient, Lora headings
   - http://localhost:5173/login - Login: Radial warm gradient (should be unchanged)
   - http://localhost:5173/profile - Profile view: Warm background, section cards with coral shadow
   - http://localhost:5173/profile/edit - Profile edit: Same warm treatment
   - http://localhost:5173/matches - Matches list: Warm background, Lora headings
   - http://localhost:5173/opportunities - Opportunities: Warm background
   - http://localhost:5173/orgs - Organizations: Warm background
   - http://localhost:5173/admin - Admin: Dot grid pattern, distinct from user pages

3. Check for visual consistency:
   - All card shadows should have subtle coral tint (not gray)
   - All page headings should use Lora serif font
   - Body text should use Plus Jakarta Sans
   - No flat gray-50 backgrounds anywhere

4. Verify responsive behavior:
   - Resize browser window - warmth should remain at all sizes
     </how-to-verify>
     <resume-signal>Type "approved" if visual treatment looks consistent and warm across all pages. Describe any issues if adjustments needed.</resume-signal>
     </task>

</tasks>

<verification>
- [ ] `grep "backgroundImage.*radial-gradient.*circle" src/routes/admin/route.tsx` shows dot grid
- [ ] `grep "bg-slate-50" src/routes/admin/route.tsx src/routes/orgs/index.tsx` returns nothing
- [ ] `grep -l "GradientBg" src/routes/orgs/index.tsx` shows the file
- [ ] Login page unchanged: `grep "radial-gradient" src/routes/login.tsx` still shows warm gradient
- [ ] `bun run lint` passes
- [ ] `bun run build` succeeds
- [ ] Human visual verification approved
</verification>

<success_criteria>

- Admin layout uses dot grid pattern with cream background
- Organizations page uses GradientBg component
- Login page warmth preserved (not changed)
- All 9 main pages have consistent warm visual treatment
- Human verification confirms visual quality
- All lint and build checks pass
  </success_criteria>

<output>
After completion, create `.planning/phases/18-core-visual-polish/18-05-SUMMARY.md`
</output>
