---
phase: 37-interactive-prompts
plan: 04
type: execute
wave: 3
depends_on:
  - '37-02'
  - '37-03'
files_modified:
  - src/routes/org/$slug/program/$programSlug.tsx
  - src/routes/org/$slug/admin/programs.tsx
autonomous: false
requirements:
  - PROMPT-02
  - PROMPT-07
  - PROMPT-09

must_haves:
  truths:
    - 'Prompts attached to a module render on the participant program page below module materials'
    - 'Facilitator can create prompts from the admin program management page'
    - 'Facilitator can view responses from the admin page'
    - 'The same PromptRenderer component is used in both module and session contexts'
    - 'The prompt system works end-to-end: create prompt, respond, save draft, submit, view responses, spotlight, reveal'
  artifacts:
    - path: 'src/routes/org/$slug/program/$programSlug.tsx'
      provides: 'Participant view with prompts rendered per module'
      contains: 'PromptRenderer'
    - path: 'src/routes/org/$slug/admin/programs.tsx'
      provides: 'Admin view with prompt creation and response viewing'
      contains: 'PromptForm'
  key_links:
    - from: 'src/routes/org/$slug/program/$programSlug.tsx'
      to: 'src/components/course/PromptRenderer.tsx'
      via: 'import and render per module'
      pattern: 'PromptRenderer'
    - from: 'src/routes/org/$slug/admin/programs.tsx'
      to: 'src/components/course/PromptForm.tsx'
      via: 'import and render in admin panel'
      pattern: 'PromptForm'
    - from: 'src/routes/org/$slug/admin/programs.tsx'
      to: 'src/components/course/PromptResponseViewer.tsx'
      via: 'import for viewing responses'
      pattern: 'PromptResponseViewer'
---

<objective>
Wire prompt components into the existing module and admin pages, then verify the full end-to-end flow.

Purpose: The prompt system components (Plans 02 and 03) need to be integrated into the actual application pages. Participants should see prompts on the program page alongside module materials. Facilitators should be able to create prompts and view responses from the admin program page. This plan also includes a human verification checkpoint to confirm the full flow works.

Output: Updated program page with prompt rendering, updated admin page with prompt management, verified end-to-end flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-interactive-prompts/37-RESEARCH.md
@.planning/phases/37-interactive-prompts/37-01-SUMMARY.md
@.planning/phases/37-interactive-prompts/37-02-SUMMARY.md
@.planning/phases/37-interactive-prompts/37-03-SUMMARY.md

@src/routes/org/$slug/program/$programSlug.tsx
@src/routes/org/$slug/admin/programs.tsx
@convex/course/prompts.ts
@convex/course/responses.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate prompts into participant program page and admin page</name>
  <files>src/routes/org/$slug/program/$programSlug.tsx, src/routes/org/$slug/admin/programs.tsx</files>
  <action>
**Participant program page (`$programSlug.tsx`):**

1. Read the existing page structure -- it shows modules with materials and progress tracking.

2. For each module section, add a prompts section below the materials list:
   - Query prompts for the module: `useQuery(api.course.prompts.getByModule, { moduleId: module._id })`
   - If prompts exist, render a "Exercises" or "Activities" subsection heading
   - For each prompt, render `<PromptRenderer promptId={prompt._id} mode="participate" />`
   - Use conditional query: pass `'skip'` if the user is not enrolled (check participation status)

3. Import `PromptRenderer` from `~/components/course/PromptRenderer`

4. Handle loading state: if prompts query is loading, show nothing (don't block module rendering)

5. Handle empty state: if no prompts for a module, render nothing (no empty state needed)

**Admin program management page (`programs.tsx`):**

1. Read the existing admin page structure -- it manages program details, modules, sessions, participants.

2. Add a "Prompts" tab or section within the module management area:
   - For each module in the admin view, add a "Prompts" subsection
   - Show existing prompts for the module: `useQuery(api.course.prompts.getByModule, { moduleId })`
   - Each prompt shows: title, field count, reveal mode badge, response count
   - "Add Prompt" button opens `PromptForm` in a dialog/sheet:
     - Pass `programId`, `attachedTo: { type: 'module', moduleId }`, `orderIndex` (next available)
     - On success: close dialog, prompts list updates reactively via Convex
   - Click on existing prompt: open `PromptResponseViewer` in a dialog/sheet
   - Delete prompt button: confirm dialog then call `api.course.prompts.remove`

3. Import `PromptForm` from `~/components/course/PromptForm`
4. Import `PromptResponseViewer` from `~/components/course/PromptResponseViewer`
5. Import `PromptRevealControl` -- include within the response viewer dialog

**Session context (future-proofing):**

- The schema supports `attachedTo: { type: 'session_phase', ... }` but session phases don't exist yet (Phase 40).
- For now, only wire module-attached prompts. Session prompts will be wired in Phase 40.
- The PromptRenderer is already context-agnostic -- it just needs a promptId -- so no changes needed there for session support.

**Important:** The existing pages may be large. Make surgical edits -- add the prompt integration without restructuring existing code. Use the project pattern of conditional useQuery with 'skip' for data that depends on access control.
</action>
<verify>Run `bun run lint` to confirm both page files compile. Start dev server (`bun run dev`) and navigate to a program page to verify prompts section appears (may be empty if no prompts created yet).</verify>
<done>Participant program page renders prompts per module. Admin page allows creating prompts per module and viewing responses. Prompt components are wired into real application routes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full end-to-end prompt flow</name>
  <files>n/a (verification only)</files>
  <action>
Human verification checkpoint. Review the complete unified prompt system end-to-end:
- Schema tables, backend CRUD with visibility logic
- Participant prompt renderer with draft/submit
- Facilitator prompt form with dynamic field builder
- Response viewer with spotlight and reveal controls
- Integration into program and admin pages
  </action>
  <verify>
Follow the how-to-verify steps below. All 17 steps must pass.
  </verify>
  <done>User confirms "approved" after verifying the full prompt lifecycle works end-to-end.</done>
  <what-built>Complete unified prompt system: schema tables, backend CRUD with visibility logic, participant prompt renderer with draft/submit, facilitator prompt form with dynamic field builder, response viewer with spotlight and reveal controls, integrated into program and admin pages.</what-built>
  <how-to-verify>
1. Start the dev server: `bun run dev`
2. Log in as a facilitator (org admin) and navigate to an org's program admin page
3. Select a module and click "Add Prompt"
4. Create a test prompt:
   - Title: "Week 1 Reflection"
   - Body: "**Reflect** on the readings and share your thoughts."
   - Add a text field: "What was your main takeaway?"
   - Add a choice field: "Difficulty level" with options "Easy", "Medium", "Hard"
   - Add a multiple-choice field: "Which topics interested you?" with 3-4 options
   - Set reveal mode to "Write Then Reveal"
   - Submit the form
5. Verify the prompt appears in the module's prompt list on the admin page
6. Switch to a participant account (or open incognito window as a participant)
7. Navigate to the program page and find the module with the prompt
8. Verify the prompt renders with markdown body, all three field types
9. Fill in partial answers and click "Save Draft" -- verify toast confirmation
10. Navigate away and return -- verify draft responses are restored
11. Complete all fields and click "Submit" -- verify toast and fields become disabled
12. Verify the visibility banner: "Your response will be visible to others after the facilitator reveals all responses"
13. Switch back to facilitator account
14. Open the response viewer for the prompt -- verify the participant's response appears
15. Click spotlight on the response -- verify SpotlightBadge appears
16. Click "Reveal All Responses" -- confirm dialog -- verify success
17. Switch back to participant -- verify other responses (if any) are now visible
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `bun run lint` passes with zero errors
- Prompts render on participant program page per module
- Admin page supports full prompt CRUD lifecycle
- End-to-end flow works: create -> respond -> draft -> submit -> view -> spotlight -> reveal
- Same PromptRenderer component used in both contexts
</verification>

<success_criteria>

- Full prompt lifecycle functional from facilitator creation to participant response to facilitator review
- Draft save and resume works across navigation
- Visibility modes correctly control what participants see
- Spotlight and reveal facilitator actions work as expected
- Human verification confirms the system works end-to-end
  </success_criteria>

<output>
After completion, create `.planning/phases/37-interactive-prompts/37-04-SUMMARY.md`
</output>
