---
phase: 39-facilitator-agent
plan: 03
type: execute
wave: 3
depends_on: [39-01, 39-02]
files_modified:
  - src/hooks/use-facilitator-agent.ts
  - src/components/facilitator-agent/FacilitatorAgentProvider.tsx
  - src/components/facilitator-agent/FacilitatorAgentSidebar.tsx
  - src/components/facilitator-agent/FacilitatorAgentChat.tsx
  - src/components/course/ProposalCard.tsx
  - src/components/course/PromptResponseViewer.tsx
  - src/components/course/FacilitatorCommentDisplay.tsx
  - src/routes/org/$slug/admin/programs/$programId.tsx
autonomous: true
requirements: [AGENT-02, AGENT-05, AGENT-06, AGENT-09]

must_haves:
  truths:
    - 'Program admin page conditionally shows facilitator sidebar instead of general admin sidebar'
    - 'Proposal cards render inside PromptResponseViewer with approve/edit+approve/dismiss actions'
    - 'Facilitator comments display below participant responses after approval'
    - 'Facilitator agent chat connects to port 3003 with program-scoped WebSocket'
    - 'Chat history loads from per-program persistence on mount'
  artifacts:
    - path: 'src/hooks/use-facilitator-agent.ts'
      provides: 'WebSocket hook for facilitator agent connection'
      exports: ['useFacilitatorAgent']
    - path: 'src/components/facilitator-agent/FacilitatorAgentProvider.tsx'
      provides: 'Context provider for facilitator agent on program pages'
      exports: ['FacilitatorAgentProvider', 'useFacilitatorAgentContext']
    - path: 'src/components/course/ProposalCard.tsx'
      provides: 'Draft proposal card with approve/edit/dismiss actions'
      exports: ['ProposalCard']
    - path: 'src/components/course/FacilitatorCommentDisplay.tsx'
      provides: 'Facilitator comment display for participant views'
      exports: ['FacilitatorCommentDisplay']
  key_links:
    - from: 'src/components/course/ProposalCard.tsx'
      to: 'convex/course/proposals.ts'
      via: 'useMutation for approve/edit/dismiss'
      pattern: 'approveProposal|editAndApproveProposal|dismissProposal'
    - from: 'src/routes/org/$slug/admin/programs/$programId.tsx'
      to: 'src/components/facilitator-agent/FacilitatorAgentProvider.tsx'
      via: 'Provider wraps program page, conditionally swaps sidebar'
      pattern: 'FacilitatorAgentProvider'
---

<objective>
Build the facilitator agent UI: WebSocket hook, chat sidebar, proposal cards inside PromptResponseViewer, facilitator comment display for participants, and conditional sidebar swap on program admin pages.

Purpose: Completes the facilitator agent user experience. Facilitators see the agent sidebar on program pages, receive proposals in context (inside the response viewer), and participants see approved comments on their responses.

Output: Full facilitator agent UI with proposal review workflow, integrated into the existing admin program page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-facilitator-agent/CONTEXT.md
@.planning/phases/39-facilitator-agent/39-01-PLAN.md
@.planning/phases/39-facilitator-agent/39-02-PLAN.md

@src/hooks/use-admin-agent.ts
@src/components/admin-agent/AdminAgentProvider.tsx
@src/components/admin-agent/AdminAgentSidebar.tsx
@src/components/admin-agent/AdminAgentChat.tsx
@src/components/admin-agent/AdminSidebarAwareWrapper.tsx
@src/components/course/PromptResponseViewer.tsx
@src/components/course/AdminModulePrompts.tsx
@src/routes/org/$slug/admin/programs/$programId.tsx
@src/routes/org/$slug/admin/route.tsx
@shared/admin-agent/constants.ts
@shared/admin-agent/types.ts
@convex/course/proposals.ts
@convex/course/facilitatorComments.ts
@convex/facilitatorAgentChat.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebSocket hook — useFacilitatorAgent</name>
  <files>src/hooks/use-facilitator-agent.ts</files>
  <action>
**Create `src/hooks/use-facilitator-agent.ts`:**

This is a close adaptation of `src/hooks/use-admin-agent.ts`. Key differences:

- Uses `FACILITATOR_AGENT_WS_PORT` (3003) instead of `ADMIN_AGENT_WS_PORT` (3002)
- Chat persistence uses `api.facilitatorAgentChat.*` instead of `api.adminAgentChat.*`
- Accepts `programId` as a parameter and passes it to persistence mutations
- No confirmation flow needed (proposals are handled in UI, not WebSocket)

```typescript
import { FACILITATOR_AGENT_WS_PORT } from '../../shared/admin-agent/constants'

export function useFacilitatorAgent(programId: Id<'programs'>) {
  // ... same structure as useAdminAgent
}

export type UseFacilitatorAgentReturn = ReturnType<typeof useFacilitatorAgent>
```

**Adapt from useAdminAgent:**

1. **Token reading**: Read from URL hash or sessionStorage (same pattern, same key `'admin-agent-token'` — the token is shared since it's the same Bun process). Actually, use a separate key `'facilitator-agent-token'` to avoid conflicts if both agents run simultaneously.

2. **WebSocket URL construction**: Use `FACILITATOR_AGENT_WS_PORT` instead of `ADMIN_AGENT_WS_PORT`. Pass same query params: `token`, `orgSlug`, `clerkToken`, `convexUrl`.

3. **Message persistence**: Replace all `api.adminAgentChat.*` calls with `api.facilitatorAgentChat.*` and pass `programId` as argument.

4. **Remove confirmation handling**: The facilitator agent doesn't use WebSocket confirmations. Proposals are handled in the UI via `useMutation`. Remove `confirm_request`/`confirm_response`/`action_result` handling from the event processor.

5. **Keep everything else**: Same reconnect logic, same token refresh, same streaming accumulation, same done/error handling.

Export the return type for the provider.
</action>
<verify>Run `bun run lint` — no type errors. Verify hook uses port 3003. Verify chat persistence passes programId. Verify no confirmation-related code.</verify>
<done>`useFacilitatorAgent(programId)` hook connects to port 3003, persists messages per-program, handles streaming without confirmation flow. Exported with return type.</done>
</task>

<task type="auto">
  <name>Task 2: Provider + Sidebar — facilitator agent UI shell</name>
  <files>src/components/facilitator-agent/FacilitatorAgentProvider.tsx, src/components/facilitator-agent/FacilitatorAgentSidebar.tsx, src/components/facilitator-agent/FacilitatorAgentChat.tsx</files>
  <action>
**1. Create `src/components/facilitator-agent/FacilitatorAgentProvider.tsx`:**

Follow `AdminAgentProvider.tsx` pattern exactly. Key differences:

- Accepts `programId: Id<'programs'>` and `orgSlug: string` props
- Uses `useFacilitatorAgent(programId)` instead of `useAdminAgent()`
- Storage keys: `'facilitator-agent-sidebar-open'` and `'facilitator-agent-sidebar-width'`
- Context type: `FacilitatorAgentContextValue` (same shape as AdminAgentContextValue but with `programId` added)
- Export `useFacilitatorAgentContext` hook

```typescript
interface FacilitatorAgentContextValue {
  isOpen: boolean
  toggle: () => void
  open: () => void
  close: () => void
  sidebarWidth: number
  setSidebarWidth: (width: number) => void
  isResizing: boolean
  setIsResizing: (v: boolean) => void
  agent: UseFacilitatorAgentReturn
  orgSlug: string
  programId: Id<'programs'>
}
```

**2. Create `src/components/facilitator-agent/FacilitatorAgentSidebar.tsx`:**

Copy `AdminAgentSidebar.tsx` structure. Key differences:

- Uses `useFacilitatorAgentContext` instead of `useAdminAgentContext`
- Renders `FacilitatorAgentChat` instead of `AdminAgentChat`
- Header title: "Facilitator Agent" instead of "Admin Agent"
- Same resizable sidebar behavior, same mobile bottom sheet

**3. Create `src/components/facilitator-agent/FacilitatorAgentChat.tsx`:**

Copy `AdminAgentChat.tsx` structure. Key differences:

- Uses `useFacilitatorAgentContext` instead of `useAdminAgentContext`
- Remove `ConfirmationCard` rendering (proposals handled elsewhere)
- Keep `TextPart`, `ToolCallCard`, `MessageBubble` subcomponents
- `DisconnectedView` shows command: `bun agent/cli.ts --org={orgSlug} --program={programId}`
- Default model selector starts on `claude-sonnet-4-6` (not Opus)

All three files go in `src/components/facilitator-agent/` directory.
</action>
<verify>Run `bun run lint` — no type errors. Verify provider exports context hook. Verify sidebar renders chat component. Verify disconnected view shows correct command with --program flag.</verify>
<done>FacilitatorAgentProvider wraps program page with context. FacilitatorAgentSidebar is resizable with mobile support. FacilitatorAgentChat streams messages without confirmation cards. Disconnected view shows correct CLI command.</done>
</task>

<task type="auto">
  <name>Task 3: ProposalCard — approve/edit/dismiss UI</name>
  <files>src/components/course/ProposalCard.tsx</files>
  <action>
**Create `src/components/course/ProposalCard.tsx`:**

This component renders a single agent proposal with action buttons.

```typescript
interface ProposalCardProps {
  proposal: {
    _id: Id<'agentProposals'>
    type: string
    content: string
    status: string
    editedContent?: string
    createdAt: number
  }
}
```

**Rendering:**

- Card with amber/yellow border to visually distinguish from regular content (agent-proposed)
- Small "AI Draft" badge in top-right corner
- Proposal content rendered as markdown (use `marked` + `dompurify` like AdminAgentChat)
- Proposal type label (e.g., "Comment", "Message", "Pair Suggestion", "Pattern Flag")

**Status states:**

- `proposed`: Show 3 action buttons:
  - **Approve** (green check icon) — calls `api.course.proposals.approveProposal({ proposalId })`
  - **Edit & Approve** — opens inline textarea pre-filled with proposal content, submit calls `api.course.proposals.editAndApproveProposal({ proposalId, editedContent })`
  - **Dismiss** (X icon) — calls `api.course.proposals.dismissProposal({ proposalId })`
- `approved`: Show green "Approved" badge, no actions, content displayed
- `edited`: Show blue "Edited & Approved" badge, show edited content
- `dismissed`: Show muted "Dismissed" badge, content faded

**Edit mode:**

- When "Edit & Approve" is clicked, replace content display with a `<textarea>` pre-filled with proposal content
- Show "Save & Approve" and "Cancel" buttons
- On save, call editAndApproveProposal mutation

**For type 'message':** After approval, add a "Copy to Clipboard" button that copies the content (per CONTEXT.md decision — no in-app messaging yet).

Use `useMutation` from `convex/react` for all actions. Show toast on success/error via `sonner`.

Follow shadcn/ui patterns: use `Card`, `Button`, `Badge`, `Textarea` components. Use `lucide-react` icons (Check, Pencil, X, Copy, Bot).
</action>
<verify>Run `bun run lint` — no type errors. Verify all 3 mutations are called correctly. Verify edit mode with textarea exists. Verify clipboard copy for message type.</verify>
<done>ProposalCard renders proposals with amber border and "AI Draft" badge. Proposed status shows approve/edit/dismiss buttons. Edit mode opens textarea. Approved/edited/dismissed states show badges. Message type has copy-to-clipboard button.</done>
</task>

<task type="auto">
  <name>Task 4: FacilitatorCommentDisplay — participant-visible comments</name>
  <files>src/components/course/FacilitatorCommentDisplay.tsx</files>
  <action>
**Create `src/components/course/FacilitatorCommentDisplay.tsx`:**

Renders facilitator comments below a participant's response.

```typescript
interface FacilitatorCommentDisplayProps {
  promptResponseId: Id<'coursePromptResponses'>
}
```

**Implementation:**

1. Query `api.course.facilitatorComments.getCommentsForResponse({ promptResponseId })`
2. If no comments, render nothing (return null)
3. For each comment, render:
   - Small card with teal/coral left border (facilitator accent)
   - Author name + timestamp
   - "AI-assisted" small badge if `fromAgent` is true
   - Comment content as text

Keep it simple — comments are short text feedback, not markdown.

Use shadcn/ui `Card` with compact styling. Import `useQuery` from `convex/react`.
</action>
<verify>Run `bun run lint` — no type errors. Verify query is called with promptResponseId. Verify fromAgent badge renders conditionally.</verify>
<done>FacilitatorCommentDisplay renders facilitator comments below responses. Shows author name, timestamp, and "AI-assisted" badge for agent-originated comments. Returns null when no comments exist.</done>
</task>

<task type="auto">
  <name>Task 5: PromptResponseViewer integration — proposals + comments</name>
  <files>src/components/course/PromptResponseViewer.tsx</files>
  <action>
**Modify `src/components/course/PromptResponseViewer.tsx`:**

This is the modal that shows all responses to a prompt. Add two integrations:

**1. Proposal cards for admin view:**

After each response in the list (inside the response mapping), for admin users:

- Query `api.course.proposals.getProposalsByTarget({ targetId: response._id })` for each response
- Render `<ProposalCard>` for each pending proposal below the response
- This shows the agent's draft comment directly below the response it references (per CONTEXT.md decision #5)

To avoid N+1 queries, consider a batch approach: query all proposals for the prompt's responses at once, then match by targetId client-side. However, the simpler approach is fine for now since response counts per prompt are typically <30.

Import `ProposalCard` from `./ProposalCard`.

**2. Facilitator comments for all users:**

Below each response, render `<FacilitatorCommentDisplay promptResponseId={response._id} />`.

- This shows approved comments to both participants and facilitators
- Import from `./FacilitatorCommentDisplay`

**3. Manual comment button for admin:**

Add a small "Add Comment" button (MessageSquare icon) next to each response for admin users.

- On click, show inline textarea
- On submit, call `api.course.facilitatorComments.addManualComment({ promptResponseId, content })`
- This allows facilitators to write comments without the agent

**Determine admin status:** The component likely already has access to an `isAdmin` prop or can be derived from the parent context. Check the existing component to see how admin-specific features (like spotlight toggle) are gated, and follow the same pattern.
</action>
<verify>Run `bun run lint` — no type errors. Verify ProposalCard renders below responses for admins. Verify FacilitatorCommentDisplay renders for all users. Verify manual comment button exists for admins. Verify existing functionality (spotlight, reveal) is not broken.</verify>
<done>PromptResponseViewer shows proposal cards below responses for admins. FacilitatorCommentDisplay renders approved comments for all users. Manual comment button opens inline textarea for facilitators. Existing spotlight and reveal functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 6: Program page — conditional sidebar swap</name>
  <files>src/routes/org/$slug/admin/programs/$programId.tsx, src/routes/org/$slug/admin/route.tsx</files>
  <action>
**Modify `src/routes/org/$slug/admin/programs/$programId.tsx`:**

Per CONTEXT.md decision #9: conditionally swap admin sidebar for facilitator sidebar on program pages.

**Approach:** The admin layout (`route.tsx`) wraps all admin pages with `AdminAgentProvider` + `AdminAgentSidebar`. For the program detail page, we need to:

1. **Add FacilitatorAgentProvider inside the program page component** (not in route.tsx — keep route.tsx unchanged to avoid affecting other admin pages):

```typescript
import { FacilitatorAgentProvider } from '~/components/facilitator-agent/FacilitatorAgentProvider'
import { FacilitatorAgentSidebar } from '~/components/facilitator-agent/FacilitatorAgentSidebar'
```

2. **Hide the admin sidebar on this page** using the `useAdminAgentContext`:
   - Call `useAdminAgentContext()` and if it's available, call `close()` on mount to hide the admin sidebar while on program pages
   - Or better: add a `hidden` state to AdminAgentProvider that program page sets

3. **Simpler approach** (preferred): In the program page component, wrap content with `FacilitatorAgentProvider` and render `FacilitatorAgentSidebar`. Use CSS to hide the admin sidebar when facilitator sidebar is active:
   - The facilitator sidebar uses the same fixed-right positioning
   - Since the facilitator agent uses port 3003 and different token key, both can coexist
   - The facilitator sidebar takes visual priority on program pages

4. **Hide admin sidebar on program pages:** Modify `src/routes/org/$slug/admin/route.tsx` to pass a context flag or use a React context that program pages can set.

**Simplest implementation:**

- In `$programId.tsx`, add `FacilitatorAgentProvider` wrapping the page content
- Add `FacilitatorAgentSidebar` at the end of the component
- Use `useAdminAgentContext()` to call `close()` on mount (to collapse admin sidebar)
- The facilitator sidebar occupies the right panel on program pages

```typescript
function ProgramDetailPage() {
  const { programId } = Route.useParams()
  const { slug } = Route.useParams()
  const adminAgent = useAdminAgentContext()

  // Close admin sidebar when on program page
  useEffect(() => {
    adminAgent.close()
  }, [])

  return (
    <FacilitatorAgentProvider programId={programId as Id<'programs'>} orgSlug={slug}>
      {/* existing page content */}
      <FacilitatorAgentSidebar />
    </FacilitatorAgentProvider>
  )
}
```

Also create `src/components/facilitator-agent/FacilitatorSidebarAwareWrapper.tsx` following the `AdminSidebarAwareWrapper.tsx` pattern — applies `margin-right` when facilitator sidebar is open.
</action>
<verify>Run `bun run lint` — no type errors. Verify program page wraps with FacilitatorAgentProvider. Verify admin sidebar closes on program page mount. Verify facilitator sidebar renders on program page. Verify other admin pages still show admin sidebar normally.</verify>
<done>Program admin page wraps content with FacilitatorAgentProvider and renders FacilitatorAgentSidebar. Admin sidebar closes on program page mount. Other admin pages unaffected. FacilitatorSidebarAwareWrapper handles content offset.</done>
</task>

</tasks>

<verification>
- `bun run lint` passes with no errors
- `src/hooks/use-facilitator-agent.ts` connects to port 3003 and persists per-program
- Facilitator agent provider, sidebar, and chat components exist in `src/components/facilitator-agent/`
- ProposalCard has approve, edit+approve, dismiss actions with proper mutations
- FacilitatorCommentDisplay shows approved comments with author names
- PromptResponseViewer renders ProposalCard below responses for admins
- PromptResponseViewer renders FacilitatorCommentDisplay for all users
- Program admin page shows facilitator sidebar, admin sidebar is hidden
- Other admin pages continue to show admin sidebar normally
- Manual comment button exists for facilitators in response viewer
</verification>

<success_criteria>
Facilitators see an AI copilot sidebar on program admin pages, can chat with it about participant progress and exercise responses, and see contextual proposals appear in the response viewer. They can approve, edit+approve, or dismiss each proposal. Approved comments become visible to participants on their response view. The experience integrates seamlessly with the existing admin agent architecture on other pages.
</success_criteria>

<output>
After completion, create `.planning/phases/39-facilitator-agent/39-03-SUMMARY.md`
</output>
